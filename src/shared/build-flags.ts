/**
 * Manual build-time switches.
 *
 * Change this before running `npm run build`.
 * true  = respect robots.txt checks
 * false = ignore robots.txt checks and fetch URLs directly
 */
export const RESPECT_ROBOTS_TX = false;
